spring:
  application:
    name: demoPaymentApp
  datasource:
    url: jdbc:mysql://localhost:3306/BatisPayment
    username: root
    password: 123132jB!
  flyway:
    enabled: true
    locations: classpath:db/migration
    clean-disabled: false

mybatis:
  configuration:
    map-underscore-to-camel-case: true